# 🖤 基于 STM32F4 的黑匣子事件回放器 - 详细开发指导方针 (MVP 初创版)

**项目代号：** BlackBox **主控芯片：** STM32F407ZGT6 (基于已有工程推断) **开发框架：** STM32 标准外设库 (SPL) **当前已有硬件：** DHT11、烟雾传感器、OLED屏、有源蜂鸣器、W25Q64存储器、EC11编码器。

## 🏗️ 一、 核心架构规范：如何管理代码？

为了防止三个人在开发时把代码改乱（Git 冲突），请严格遵守以下工程规范：

1. **分模块建文件夹：** 在你们的 `User/` 目录下，不要把所有的 `.c` 和 `.h` 都堆在一起。继续保持现在的良好习惯，新建如 `User/sensor/`, `User/oled/`, `User/storage/` 等文件夹。
2. **`main.c` 洁癖：** `main.c` 里**绝对不要**写底层的寄存器配置或具体的硬件逻辑！`main.c` 只做两件事：
   - 在 `main()` 开头调用各种 `bsp_xxx_init()`。
   - 在 `while(1)` 里跑一个清晰的**状态机**（正常巡检、报警态、回放态）。
3. **全局数据黑板：** 新建一个 `User/core/bsp_blackbox.h`。把所有模块都需要访问的全局结构体和枚举定义在这里，谁需要用谁就 `#include "bsp_blackbox.h"`。

## 💾 二、 灵魂一步：定义全局数据结构 (基于现有器件)

在 `bsp_blackbox.h` 中写入以下内容。这是你们三人协作的“契约”，所有人都要围绕这个结构体写代码：

```
#ifndef __BSP_BLACKBOX_H
#define __BSP_BLACKBOX_H

#include "stm32f4xx.h"

// 系统当前运行状态
typedef enum {
    STATE_NORMAL = 0,   // 正常巡检，屏幕显示实时环境
    STATE_ALARM,        // 烟雾触发危险，蜂鸣器报警，后台存数据
    STATE_REPLAY        // 旋钮按下进入回放模式，查看历史记录
} SystemState_t;

// 黑匣子事件记录结构体 (精简版，完美适配当前硬件)
typedef struct {
    uint32_t timestamp;      // 时间戳 (由于没借RTC模块，前期可用基本定时器自增模拟秒数)
    uint8_t  trigger_src;    // 触发源: 1-烟雾超标 (留足空间给未来加外设)
    
    // 事故瞬间环境数据
    float    temp;           // 峰值温度 (DHT11)
    float    humi;           // 峰值湿度 (DHT11)
    uint16_t smoke_val;      // 烟雾 ADC 模拟量数值
    
    // 联动执行状态
    uint8_t  buzzer_status;  // 0-安静, 1-警报鸣叫中
} BlackBoxEvent_t;

// 声明一个全局状态机变量供其他文件 extern 使用
extern SystemState_t g_SystemState;

#endif
```

## 👨‍💻 三、 三人“铁三角”极速开发分工

现有的 6 个器件刚好可以完美划分为三大独立模块，你们三人可以**完全并行开发，互不干扰**：

### 🧑‍🔧 成员 A：安防感知与警报执行 (主攻 ADC 与时序)

- **负责外设：** DHT11温湿度, 烟雾传感器, 有源蜂鸣器。
- **文件路径：** 新建 `User/sensor/bsp_sensor.c`, 蜂鸣器复用 `bsp_led.c` 或新建 `bsp_buzzer.c`。
- **核心任务与考点：**
  1. **DHT11 单总线时序：** 严格按照数据手册写微秒级延迟（可用已有 `bsp_basic_tim.c`），把温湿度读出来。
  2. **烟雾 ADC+DMA：** 烟雾传感器输出的是模拟电压。你要配置 ADC1，并**必须挂载 DMA 传输**（这是拿高分的核心考点），将烟雾浓度实时搬运到内存变量中。
  3. **阈值与报警：** 在代码里写一个判定函数 `Check_Alarm()`，如果烟雾 ADC 值大于设定阈值，立刻把全局变量 `g_SystemState` 改为 `STATE_ALARM`，并拉高引脚让蜂鸣器狂响。

### 🧑‍💻 成员 B：黑匣子“记忆”中枢 (主攻 SPI 协议与算法)

- **负责外设：** W25Q64 存储器。
- **文件路径：** 新建 `User/w25q64/bsp_spi_flash.c`。
- **核心任务与考点：**
  1. **底层 SPI 通信：** 调通 STM32 与 W25Q64 的硬件 SPI 读写（读 ID、擦除扇区、页写入）。
  2. **环形存储逻辑（全组最难）：** 构思怎么把 `BlackBoxEvent_t` 结构体存进 Flash，且不会把空间撑爆。
  3. **封装高级接口：** 给另外两名组员提供两个傻瓜式函数：
     - `BlackBox_SaveEvent(BlackBoxEvent_t *event)`：A 触发警报时调用。
     - `BlackBox_ReadEvent(uint32_t index, BlackBoxEvent_t *event)`：C 回放时调用。

### 🧑‍🔬 成员 C：人机交互操作台 (主攻 UI 排版与中断)

- **负责外设：** OLED屏, EC11编码器。
- **文件路径：** 新建 `User/oled/bsp_oled.c`, `User/ec11/bsp_ec11.c`。
- **核心任务与考点：**
  1. **EC11 旋钮解码：** **重点！** 不要用普通按键扫描去读旋钮，查阅手册把对应的两个引脚配置为 **TIM 编码器模式 (Encoder Mode)**，让硬件自动计数值。按压引脚配置为**外部中断 (EXTI)**。
  2. **多状态 UI 引擎：** 根据 `g_SystemState` 刷新屏幕。
     - 正常态：显示大字体的温度、湿度、烟雾浓度进度条。
     - 报警态：全屏闪烁“DANGER! 正在记录...”
     - 回放态：通过旋转 EC11 翻页，调用 B 写好的 `ReadEvent` 函数，把历史事故的各项数据漂亮地排版在屏幕上。

## 🚀 四、 实施四步曲（从 MVP 到完全体）

请严格按照以下顺序合并代码，切忌一口气全接上：

- **阶段一（首周目标 - 各自为战）：**
  - A 组装 DHT11 和烟雾，能在串口调试助手里看到温湿度和 ADC 数据跳动。
  - B 能把一组假数据写进 W25Q64 的 0x000000 扇区，再原封不动读出来打印到串口。
  - C 点亮 OLED，能转动 EC11 让屏幕上的数字 `+1` 或 `-1`。
- **阶段二（联调闭环 - 黑匣子诞生）：**
  - 把 A、B、C 的代码合并进 `main.c`。
  - 流程打通：用打火机放气触发 A 的烟雾阈值 -> A 触发警报 -> B 自动把此刻 A 读到的数据写入 Flash -> C 按下旋钮，屏幕进入回放模式，读出刚才写入的数据。
- **阶段三（引入看门狗与优化）：**
  - 在 `main()` 的 `while(1)` 里加入你们已经准备好的 `bsp_iwdg.c` (独立看门狗) 喂狗代码。
  - 优化代码格式，准备撰写报告，突出你们用到了 DMA、定时器、中断和看门狗。
- **阶段四（看情况进阶）：**
  - 前三步做完，如果还有充足的时间和预算，再去考虑借继电器、蓝牙等扩展设备。如果没有，目前这个版本只要演示丝滑，绝对足够拿高分了！

祝代码一次编译通过！(0 Errors, 0 Warnings)